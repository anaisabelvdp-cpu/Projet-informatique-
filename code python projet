import random  #générer des éléments aléatoires.

#Couleurs possibles
couleurs = ["rouge", "bleu", "vert", "jaune", "orange", "violet"]

# Générer la combinaison secrète
combinaison = []
for i in range(4):
    combinaison.append(random.choice(couleurs))

print("Jeu du Mastermind")
print("Couleurs possibles :", couleurs)

# Demander une proposition au joueur
entree = input("Entre 4 couleurs séparées par un espace : ")
proposition = entree.split()

bien_places = 0
mal_places = 0

# Comparaison des couleurs

#print les couleurs si elle sont bien places ou mal place
# On compare les couleurs de l’ordinateur à celle du joueur

For i in range(4): 
       if proposition[i] == combinaison[i]
             bien_places +=1 

# Si couleur mal placé alors vérifier si elle est présente dans la combinaison secrète.

elif proposition[i] in combinaison: 
      mal_places +=1 

# Dire au joueur le nombre de pions bien placé 

print(“Tu as” , bien_places, “couleurs bien placé”)

print(“Tu as” ,mal_places, “couleurs mal placé mais présente dans la combinaison”)

# Vérifier si le joueur a gagné 

If bien_places == 4:
    Gagne = True
     print(“Bravo tu as remporté la partie”)

Or boucle

else victoire = False:
         print(“Dommage, tu as perdu”)
